if [ "$(uname)" = "Linux" ]; then
	# I am dumb and need this, sorry
	alias yay=paru
fi

if command -v corepack >/dev/null; then
	alias yarn="corepack yarn"
fi

alias btw=fastfetch
alias openports="lsof -nPi"
